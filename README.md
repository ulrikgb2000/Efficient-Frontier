# Efficient-Frontier

Fintech project 

Required packages that can be installed with PIP:
* yfinance
* numpy
* matplotlib

For stock a and b
Portfoilo covariance = (Wa)^2 * (SDa^2) + (Wb)^2 * (SDb^2) + 2 * Wa * Wb * Cov(a, b)

Also, remember that 
SD(a, b) = Cov(a, b) / (SDa * SDb)